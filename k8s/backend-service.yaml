apiVersion: v1
kind: Service
metadata:
  name: diabetes-backend-service
spec:
  selector:
    app: diabetes-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP