apiVersion: v1
kind: Service
metadata:
  name: diabetes-frontend-service
spec:
  selector:
    app: diabetes-frontend
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
  type: NodePort